services:
  - type: web
    name: syncyt
    env: node
    plan: free
    buildCommand: npm run install-all
    startCommand: npm run server
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /